[project]
name = "respiratory-audio-diploma"
version = "0.1.0"
description = "Respiratory audio classification experiments for the diploma project."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

